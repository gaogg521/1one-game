# 2026-05-17 迭代总结

## 交付摘要

面向「终端用户不应配 .env 业务参数」与「可自动化代替手测」，本日完成产品配置内聚、长篇小说分段续写、共创精炼闭环、六模板玩法深化及全量 QA 基建，并已推送 `main`（`1e44a87`）。

## 1. 产品配置（`src/lib/product-config.ts`）

- **模型**：游戏 `gpt-5.2`、小说/漫画 JSON `deepseek-v4-pro`、文生图 `gpt-image-2` 等均在代码中维护。
- **小说**：短篇/中篇/长篇超时、输出 token、最低字数；长篇分段见 `novel-long-config.ts`（每段 1 万字、目标 8 万字、最多 24 段）。
- **漫画**：配图并发 4、批量 `n=4`。
- **游戏**：规格生成/修补/强化超时、lint repair 2 轮。
- **API**：请求体 512KB、单机限流窗口与上限。
- **`.env` 仅保留**：`OPENAI_API_KEY`、`OPENAI_BASE_URL`、可选 `GEMINI_API_KEY`、`COMFY_UI_BASE_URL`、E2E/日志开关。

## 2. 小说

- **长篇分段续写**：先大纲（人物/矛盾/分段时间线），再多段流式续写；携带最近 6 章摘要 + 上段末尾 ~2800 字。
- **中篇流式断连**：根因网关 `x-openclaw-timeout-ms=25s`；改为代码默认 600s，小说客户端按篇幅覆盖（长篇 30min）。
- **Prisma 漂移兜底**：`lengthTier`、`coverPath`、`refinementLogJson` 写入 raw SQL 优先。

## 3. 游戏

- **创作台 4 步共创**：意图 → 方向 → 生成。
- **`POST /api/projects/[id]/refine`**：patch / regenerate + `refinementLogJson`。
- **PlayScene / PlatformerScene / ShooterScene**：`director` 目标闭环、关卡段落、Boss/精英波等。
- **六模板**：collector / survivor / avoider 生成字段与运行时深化。

## 4. 漫画与 Studio

- 分镜 JSON 分段请求（缓解中篇 8 页 502）。
- **小说/漫画 duplicate API** + 工作室「复制副本」。
- 文生图：≤4 格批量、>4 格并发；SSE 配图进度。

## 5. 测试与资产入库

| 项 | 说明 |
|----|------|
| `npm run qa:full` | migrate + build + ci.sqlite 服务 + 离线 QA + E2E + 手测 |
| Playwright | **24/24**（`PW_EXTERNAL=1`、`E2E_REFINE_STUB=1`） |
| `qa:template-matrix` | 六模板 6/6 |
| `prisma/ci.sqlite` | E2E/手测用库（随仓库提交，无密钥） |
| `public/covers/openai-*.png` | 网关文生图回归样例（无用户隐私） |

## 6. 已知限制 / 下一步

- 32 格全配图仍约 40～50 分钟，需长连接与耐心。
- 肉眼扫一眼六模板章节动效（E2E 无法替代）。
- 工作室批量删除待做。

## 仓库

- https://github.com/gaogg521/1one-game  
- 代码：`1e44a87` — product-config、长篇分段、refine、六模板等  
- 文档与测试资产：`e79bd50` — README、日结、`ci.sqlite`、`openai-*.png` 回归封面
