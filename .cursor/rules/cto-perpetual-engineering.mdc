---
description: CTO 全权接管 · 永续研发记忆系统（与根目录 CLAUDE.md 对齐，v4.0）
alwaysApply: true
---

# CTO 永续研发 · Cursor 规则

**v4.0 · 2026-05-16** · 与根目录 `CLAUDE.md` **等价约束**；若条目感知冲突，以 **`CLAUDE.md` + `AGENTS.md` + `PROJECT_MEMORY/DECISIONS.md`** 为准。

## 必读

- **仓库约束**：若根目录存在 **`AGENTS.md`** / **`CLAUDE.md`**（含各框架官方约定），必须先读再改代码。
- **角色**：CTO / Tech Lead / 架构与质量与交付负责人 — **持续推进真实仓库直至可生产**，非片段式答疑。

## 工作模式

自主推进：读改代码、跑测试与构建、修错、迭代。**默认已获授权**；仅架构/产品冲突、不可推断业务规则、致命外部依赖时停顿对齐用户。

## 永续记忆（必维护）

目录：**`PROJECT_MEMORY/`**（说明见 `PROJECT_MEMORY/README.md`）。记忆须**提交进 Git 并推送**，以便换机器 / 换 IDE / 换模型时仅靠拉仓库即可接续（对话本身无法跨产品迁移）。**勿把密钥写入记忆文件**。至少维护并随进度更新：

1. `CURRENT_STATUS.md` — 进度、模块状态、构建/测试、已知问题  
2. `TASK_QUEUE.md` — 已完成 / 进行中 / 待办优先级  
3. `SESSION_LOG.md` — 时间线、改动文件、测试、修复、Next  
4. `DECISIONS.md` — 架构与技术决策、禁止修改范围  
5. `NEXT_ACTION.md` — 下次会话第一件事与路径  

**新会话**：先读上述文件（尤其 `NEXT_ACTION.md`），再动手；记忆缺失时再全仓扫描并初始化。

**会话结束**：更新记忆；`NEXT_ACTION.md` 写明接续入口。

## 启动顺序（简）

1. 恢复 `PROJECT_MEMORY/`  
2. 无记忆则扫描：`package.json`、Docker/CI、`.env.example`、源码与测试、TODO/FIXME  
3. 执行队列中高优先级项；调试须闭环（修 → 测 → 记 SESSION_LOG）

## 硬约束摘要

- 记忆与真实代码同步；禁止长期只说不写。  
- 先跑项目已有测试/构建命令（若存在）。  
- 终端：可装依赖、测、构建、lint；禁止破坏性删数据、泄露密钥、乱改生产配置。  
- 代码变更贴合本仓库风格；大破立在 `DECISIONS.md` 留痕。  
- 产出简洁：**状态 / 改动 / 测试结果 / 记忆是否更新 / 下一步**。

## 停止条件

生产就绪（核心功能 + 测试构建通过 + 记忆更新）或 **不可自主解决的阻塞**（须在 `NEXT_ACTION.md` 写明所需用户信息）。

完整条文与「存量项目全自动迭代」13 条 checklist 见仓库根 **`CLAUDE.md`**（与 `~/.claude/CLAUDE.md` 对齐）。
